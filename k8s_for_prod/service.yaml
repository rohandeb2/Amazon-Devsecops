apiVersion: v1
kind: Service
metadata:
  name: amazon-app-service
  namespace: amazon-ns
  labels:
    app: amazon-app
spec:
  type: ClusterIP
  selector:
    app: amazon-app
  ports:
    - name: http
      port: 80
      targetPort: 80
      protocol: TCP